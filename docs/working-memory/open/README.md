# Open tasks

Active Ralph tasks live here. Each task has:

- `plan.md` — PRD/context
- `user_story.json` — Stories with acceptance criteria
- `updates.md` — Progress log

**When all stories pass:** move the task folder to `done/`, commit, and stop. See `ralph/TASK-STRUCTURE.md`.
