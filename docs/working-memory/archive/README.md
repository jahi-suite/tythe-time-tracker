# Archived tasks (plan-only)

These tasks have only `plan.md` (no `user_story.json`). Ralph now requires all tasks to use plan.md + user_story.json.

To run one: move it to `open/`, add `user_story.json` per `ralph/TASK-STRUCTURE.md`, then `./ralph/run.sh <task-id>`.
