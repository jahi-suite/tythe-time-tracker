# Updates: tt-export-admin-20260223

- Task created. Goal: managers and admins see Manager Export Access in Streamlit export page.
- Fixed `export_interface._is_manager()` to treat both `manager` and `admin` as manager-level export access.
