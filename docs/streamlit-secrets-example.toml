# Streamlit Cloud Secrets — paste into Settings → Secrets (valid TOML).
# Use this as a reference; do not commit real passwords.

[SUPABASE]
HOST = "aws-1-eu-central-1.pooler.supabase.com"
DATABASE = "postgres"
USER = "postgres.nfwzrlxhjcxdznsnkhhm"
PASSWORD = "your-database-password"
PORT = "5432"

# First manager account (created when users table is empty)
SEED_MANAGER_USERNAME = "admin"
SEED_MANAGER_PASSWORD = "your-secure-password"

# Legacy single manager password (optional)
MANAGER_PASSWORD = "tythe2024"
