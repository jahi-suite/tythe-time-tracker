# Copy values from tt-ts/.env into Cloud Run env vars.
# Use SUPABASE_PORT=6543 (pooler) for serverless.
# Add NODE_ENV=production and SESSION_STORE=pg.

SUPABASE_HOST=
SUPABASE_DATABASE=postgres
SUPABASE_USER=
SUPABASE_PASSWORD=
SUPABASE_PORT=6543
SESSION_SECRET=
NODE_ENV=production
SESSION_STORE=pg
